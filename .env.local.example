# Frontend Environment Variables
# Copie este arquivo para .env.local e preencha os valores

# URL da API do Backend (obrigatório)
# Em desenvolvimento: http://localhost:3001
# Em produção: URL do seu backend no Railway (ex: https://seu-backend.railway.app)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Nota: Todas as variáveis que começam com NEXT_PUBLIC_ são expostas ao cliente (navegador)
# Não coloque valores sensíveis (senhas, tokens secretos) em variáveis NEXT_PUBLIC_*
